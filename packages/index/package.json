{
  "name": "index",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "subset-fonts": "glyphhanger ./index.html --subset=\"./assets/fonts/SpaceGrotesk.woff2\" --formats=woff2,woff",
    "dev": "rimraf dist && parcel",
    "build": "rimraf dist && parcel build"
  },
  "devDependencies": {
    "@parcel/optimizer-data-url": "^2.6.0",
    "@parcel/transformer-inline-string": "^2.6.0",
    "glyphhanger": "^5.0.0",
    "parcel": "^2.6.0",
    "parcel-transformer-meta-image": "^1.0.4",
    "rimraf": "^3.0.2"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "@parcel/transformer-css": {
    "drafts": {
      "nesting": true
    }
  }
}
